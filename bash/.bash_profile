#
# Bash profile configuration file
#
# File location: ~/.bash_profile
#
# When bash is started, it first executes /etc/profile if existing, after this
# it looks for ~/.bash_profile and execute it.
#

# If bash has a configuration file, execute its content.
[[ -f ~/.bashrc ]] && . ~/.bashrc

